type ParameterValue = boolean | number | string | undefined;

type FetcherParameters = Record<string, ParameterValue>;

export type { FetcherParameters as FetcherParams, ParameterValue as ParamValue };
